apiVersion: cert-manager.io/v1beta1
kind: Certificate
metadata:
  name: myapp-certificate
spec:
  dnsNames:
  - 'zhg.dacdevops.ru'
  issuerRef:
    kind: ClusterIssuer
    name: yc-clusterissuer
  secretName: myapp-cert